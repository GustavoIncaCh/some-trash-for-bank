enum Pages{
  none,
  updated,
  warning,
  seeds,
  keys,
  terms,
  waiting,
  change,
}

///Pin action enums
enum Process{
  register,
  change,
  check,
  login,
  confirm,
}

///Pin indicators
enum Pointer{
  number,
  empty,
  full,
}

enum Countries{
  pe,
  br,
}

enum Languages{
  es,
  pt,
}

enum Prefix{
  es,
  pt,
}